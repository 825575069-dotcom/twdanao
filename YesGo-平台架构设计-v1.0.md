# YesGo AI 数字员工平台 — 总体架构设计

> 版本：v1.0  
> 来源：基于用户 2026-07-23 提供的平台架构图整理  
> 定位：后续平台开发的顶层架构蓝本  

---

## 1. 架构总览

YesGo 平台采用 **「三层架构」**：

```
┌─────────────────────────────────────────────────────────────────┐
│ 第一层：客户触点 / 数据底座接入层（外部系统）                      │
│   商城 · B2B/B2C/三方平台后台 · 客户 SaaS · 客户 ERP · 其他系统    │
└─────────────────────────────────────────────────────────────────┘
                              ↓ 数据底座API
┌─────────────────────────────────────────────────────────────────┐
│ 第二层：天网大脑 / 总后台中台总控（平台核心）                      │
│   公共数据库(采购) · 租户数据库 · AI配置/租户管理 · 大模型接入      │
└─────────────────────────────────────────────────────────────────┘
                              ↓ 前端交互API
┌─────────────────────────────────────────────────────────────────┐
│ 第三层：YesGO 前端交互 / 中控后台（用户终端）                      │
│   SaaS 版 · 独立部署版（源码交付）                                │
└─────────────────────────────────────────────────────────────────┘
```

---

## 2. 第一层：客户触点 / 数据底座接入层

### 2.1 外部系统分类

| 类型 | 代表系统 | 接入内容 |
|------|---------|---------|
| **电商平台** | 商城（多个） | 商品 SKU、价格、库存、订单 |
| **B2B 客户后台** | 医药商业公司系统 | 客户档案、采购订单、应收应付 |
| **B2C 客户后台** | 零售药房/诊所系统 | 会员、零售订单、售后 |
| **三方平台后台** | 天猫/京东/拼多多等 | 平台订单、促销、物流 |
| **客户 SaaS 后台** | 莱芬享 / 类似系统 | 已有 SaaS 业务数据 |
| **客户 ERP 内部系统** | 用友/金蝶/自研 ERP | 财务、进销存、供应链 |
| **客户其他系统** | CRM、WMS、BI 等 | 客户关系、仓储、报表 |

### 2.2 与平台的数据关系

```
外部系统 ──数据底座API──→ 天网大脑
              ↑
         可挑选产品进入公共数据库
```

- **上行**：外部系统把商品、库存、订单、客户、流向等数据通过「数据底座API」同步到天网大脑。
- **下行**：天网大脑把补货方案、客户跟进计划、价格策略等结果回写到外部系统。
- **公共数据库**：可跨租户复用的标准商品/采购数据，由平台运营方维护。

---

## 3. 第二层：天网大脑 / 总后台中台总控

这是整个平台的**核心中台**，承担数据、AI、租户、配置的集中管理能力。

### 3.1 核心模块

| 模块 | 职责 | 对应现有代码 |
|------|------|-------------|
| **公共数据库（采购）** | 平台级标准商品库、采购目录、供应商池 | `mockSaaS.ts` 商品/供应商数据（待接入真实 API） |
| **租户数据库** | 按企业信用代码隔离的租户数据 | `appStore.tenant`（待接入后端租户服务） |
| **运营/流向/跟客/学术** | 各垂直业务数据表 | `DataBaseView` 连接器 / `DataView` 看板 |
| **AI 相关配置** | 模型网关、Dify 工作流、Prompt、Agent 参数 | `ModelsView` / `ConfigView` / `lib/dify.ts` |
| **租户管理** | 租户开通、套餐、成员、角色、积分 | `PermissionsView` / `appStore` |
| **大语言模型接入** | 统一对接商业 LLM / 开源 LLM | `lib/dify.ts` / `lib/api.ts` |

### 3.2 大模型双通道

```
        ┌─────────────────┐
        │  商业大语言模型  │ ←── API 接入（如 OpenAI/文心/通义等）
        └────────┬────────┘
                 │
        天网大脑（总控层）
                 │
        ┌────────┴────────┐
        │  开源大语言模型  │ ←── 云部署（私有部署 / 本地化）
        └─────────────────┘
```

- **商业 LLM**：通过 API 调用，适合通用意图识别、文本生成。
- **开源 LLM**：云部署或私有化部署，适合对数据安全要求高的医药场景。
- **天网大脑**负责统一调度：根据任务类型、成本、安全要求选择模型。

---

## 4. 第三层：YesGO 前端交互 / 中控后台

这是用户直接使用的终端层，向下通过「前端交互API」调用天网大脑。

### 4.1 两个产品形态

| 形态 | 定位 | 交付方式 | 对应现有项目 |
|------|------|---------|-------------|
| **YesGO AI 企业中台 SaaS 版** | 多租户共享平台，开箱即用 | 云服务，按租户订阅 | `ai-employee-app` 的 Web/H5 版本 |
| **YesGO AI 企业中台独立版** | 单租户私有化部署 | 源码交付，客户自建 | `ai-employee-app` 的 Electron 桌面版 |

### 4.2 终端层内部模块

```
YesGO 终端
├── 对话工作台（ChatView）
├── AI 办公室（AgentOfficeView）
├── 数据底座（DataBaseView）
├── 经营看板（DataView）
├── 客户管理（ClientsView）
├── 权限/积分/模型/配置（Permissions/Credits/Models/Config）
└── 素材/任务/知识库（Media/Tasks/Knowledge）
```

---

## 5. 数据流全景

```
[用户说话] 
    ↓
[YesGO 终端] ──前端交互API──→ [天网大脑]
    ↓                              ↓
[展示结果]                 [意图识别]
                              ↓
                    [选择智能体 + 选择模型]
                              ↓
                    [读取数据底座 / 公共数据库]
                              ↓
                    [生成方案 / 执行任务]
                              ↓
                    [回写外部系统 / 返回终端]
```

---

## 6. 与现有项目的映射

当前项目 `ai-employee-app` 目前属于 **第三层终端 + 部分第二层 Mock**。后续按此架构补齐：

| 架构层 | 已有实现 | 待接入/待建 |
|--------|---------|------------|
| 第一层外部系统 | `mockSaaS.ts`（Mock 数据） | 真实数据底座 API、OAuth/开放平台对接 |
| 第二层天网大脑 | `appStore`（本地状态模拟）、`lib/dify.ts`（Mock） | Django 后端、PostgreSQL、Dify 引擎、模型网关 |
| 第三层终端 | Electron 桌面端 + 刚创建的 H5 移动端 | SaaS Web 版、独立部署版打包 |

---

## 7. 后续开发路线（按架构补齐）

### Phase 1：终端层完善（当前重点）
- 完成 Electron 桌面端功能闭环
- 完成 H5 移动端体验优化
- 统一 UI 组件库，确保桌面/H5 代码复用

### Phase 2：天网大脑后端建设
- 搭建 Django 多租户后端
- 租户/成员/角色/积分/模型配置 API
- Dify 工作流对接（5 大智能体）
- 公共数据库 + 租户数据库设计

### Phase 3：数据底座对接
- 与莱芬享/客户 SaaS 对接
- 与 B2B/B2C/三方平台后台打通
- 商品/库存/订单/流向/客户数据同步

### Phase 4：大模型接入
- 商业 LLM API 接入
- 开源 LLM 云部署
- 模型路由与成本监控

### Phase 5：商业化交付
- SaaS 版多租户上线
- 独立版源码交付流程
- 部署文档与运维监控

---

## 8. 关键术语对照

| 图中名称 | 含义 | 对应英文/代码名 |
|---------|------|----------------|
| 天网大脑 | 平台中台总控 | YesGo Brain / Central Hub |
| 数据底座 API | 外部系统接入接口 | Data Foundation API |
| 前端交互 API | 终端调用中台的接口 | Frontend API |
| 公共数据库 | 平台级共享数据 | Public DB |
| 租户数据库 | 按企业隔离的数据 | Tenant DB |
| 莱芬享 | 客户已有 SaaS 系统 | 示例客户系统 |

---

## 9. 设计原则

1. **中台集中**：所有 AI 能力、数据治理、租户管理统一在天网大脑。
2. **终端轻量**：YesGO 终端只负责交互展示，不存储核心业务数据。
3. **数据隔离**：租户数据库严格按企业信用代码隔离。
4. **模型可替换**：通过模型网关，支持商业 LLM 和开源 LLM 灵活切换。
5. **源码可交付**：独立版保证可私有化部署，不依赖平台 SaaS。
