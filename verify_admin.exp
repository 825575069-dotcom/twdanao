#!/usr/bin/expect -f
set timeout 30
set host "47.112.156.183"
set user "root"
set password "\$8kL#mP2@666"

spawn ssh -o StrictHostKeyChecking=no $user@$host
expect "password:" { send "$password\r" }
expect "# "

send "curl -s https://twdanaob.88yldh.com/admin/ -k | head -10\r"
expect "# "
send "curl -s -X POST https://twdanaob.88yldh.com/api/v1/auth/login/ -k -H 'Content-Type: application/json' -d '{\"username\":\"chensheng\",\"password\":\"chensheng123\"}' 2>&1 | head -200\r"
expect "# "
send "echo VERIFY_ADMIN_DONE\r"
expect "VERIFY_ADMIN_DONE"
