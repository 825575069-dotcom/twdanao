#!/usr/bin/expect -f
set timeout 30
set host "47.112.156.183"
set user "root"
set password "\$8kL#mP2@666"

spawn ssh -o StrictHostKeyChecking=no $user@$host
expect "password:" { send "$password\r" }
expect "# "

send "ls -la /etc/nginx/conf.d/twdanao* 2>/dev/null\r"
expect "# "
send "lsattr /etc/nginx/conf.d/twdanao* 2>/dev/null\r"
expect "# "
send "nginx -t 2>&1\r"
expect "# "
send "curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1/admin/\r"
expect "# "
send "curl -s -o /dev/null -w '%{http_code}' https://twdanaob.88yldh.com/admin/ -k\r"
expect "# "
send "echo NGINX_CHECK_DONE\r"
expect "NGINX_CHECK_DONE"
