# 双图标服务入口执行计划

## 目标

将右下角组合服务菜单替换为常驻的意见反馈与联系客服两个独立圆形图标，保留既有弹窗与返回顶部行为。

## 阶段

- [x] 1. 编写并运行两个常驻入口的失败测试。
- [x] 2. 移除展开菜单状态，改为两个直接触发的圆形图标。
- [x] 3. 更新旧菜单测试并完成定向、完整测试与构建。
- [ ] 4. 在浏览器验证图标排列与两个弹窗（浏览器连接超时，待恢复后补验）。

## 约束

- 不修改反馈提交、客服电话/在线咨询、返回顶部阈值或弹窗内容。
- 工作于 `codex/system-log-tenant-feedback`，不修改 `dev`。
