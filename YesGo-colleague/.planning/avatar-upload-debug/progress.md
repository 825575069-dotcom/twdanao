# 进度记录

## 2026-07-17

- 已开始按系统化排障流程检查上传链路。
- 已确认工作区初始 `git status --short` 无输出。
- 首次恢复脚本路径不存在，未重复执行；后续改用当前技能实际目录继续。
- 已确认 `oss2` 未安装且未在 `server/pyproject.toml` 声明；这是当前真实上传失败的根因。
- 已新增 OSS 失败响应的回归测试并验证 RED：测试以未捕获 `RuntimeError` 失败。
- 已将 `oss2>=2.19,<3.0` 加入 `server/pyproject.toml`，并在共享上传入口将 OSS 运行时依赖错误转为 `503 / code=1006` 标准响应。
- 回归测试 GREEN：`test_control_avatar_upload_returns_service_unavailable_when_oss_upload_fails` 通过；上传相关后端测试共 30 条通过。
- 已在当前 `.venv` 安装并确认可导入 `oss2 2.19.1`。
- 验证：`pytest -q` 成功完成；迁移检查输出 `No changes detected`；Web 测试 45 文件、309 条通过；Web 构建成功。
- Web 测试存在既有 Vue 组件解析警告，构建存在既有大包警告，均未导致测试或构建失败。
- 用户已确认新的头像交互：选择后本地预览，点击保存时才上传；上传失败时保留待上传文件供重试。
- 已完成组件与账户页改造：保存按钮与头像叠层在上传或保存期间显示 loading，重复选择和重复提交均被禁用。
- 回归验证：头像组件测试通过；个人中心测试 14 条通过；全量 Web 测试 45 文件、309 条通过；生产构建成功。
- OSS 只读验证输出 `OSS_READ_OK`。运行中的后端若仍返回 503，应重启该进程；Docker 部署需重建后端镜像，使其安装已声明的 `oss2` 依赖。
- 2026-07-20：已执行本地 SQLite 迁移，随后启动 Django 后端（PID 27364）及 Vite 前端（PID 4164）。后端 `GET /api/health/` 返回 200，前端首页返回 200。
- 已新增手机号重复场景回归测试，先验证 RED（提示仍为泛化“保存失败”），再实现字段错误提示与焦点定位；个人中心测试现为 15 条通过。
- 字段级校验实现：当接口返回 `code=1001` 与 `data.mobile` 时，页面提示“保存失败，手机号已存在”，手机号控件显示红色帮助文案并自动获得焦点；用户修改手机号后会清除该字段错误。
- 验证：全量 Web 测试 53 个文件、383 条通过；`yarn build` 成功。Django 迁移检查输出 `No changes detected`。本轮后端全量测试因终端回传窗口仅保留进度片段，未将其计为新的通过依据；此前该活动计划记录的全量后端测试已通过。
- 已新增通用 `api-field-errors` 工具与 `StandardEditForm` 服务端错误回填能力；`code=1001` 的字段错误会提示“保存失败，具体原因”，并标红、滚动和尝试聚焦首个对应控件。资料页、角色、权限、部门和员工详情页均已接入。
- 先新增解析回归测试并验证 RED（模块不存在），实现后相关测试 103 条通过；生产构建首次发现两处 TypeScript 类型问题，已补齐类型声明后通过。全量 Web 测试 54 个文件、386 条通过；生产构建成功。
- 合并 `codex/tenant-agent-package` 前，全量 Web 测试发现总控系统设置页的既有断言与已合并的提示文案不一致；已将测试期望更新为“加载失败，请稍后重试：已保留当前设置内容”，运行逻辑未改动。
