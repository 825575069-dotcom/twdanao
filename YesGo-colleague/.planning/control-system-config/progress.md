# 进度记录

## 2026-07-10

- 已完成：初步检索总控“系统管理”相关菜单、路由、权限和页面映射。
- 已确认：当前总控“系统设置”尚未实现独立系统级配置页面，现有“集成配置”页面承载的是租户站点配置。
- 已完成：新增 `system_config` 模型、总控系统设置 API、前端左侧导航式系统设置页面，并移除总控“集成配置”菜单入口。
- 已验证：`makemigrations --check --dry-run`、`yarn test -- --runInBand`、`yarn build` 通过；后端 `pytest` 首轮发现初始化 SQL 种子未同步，已修复后准备复跑。
