# 管理端 RBAC 边界执行计划

## 目标

落实总控与租户超级管理员专属的权限、角色、部门管理边界，并将员工操作“授权”改为“角色”。

## 阶段

- [x] 1. 调研现有菜单、路由守卫与后端权限实现，确定超级管理员判定来源。
- [ ] 2. 编写前后端失败测试，覆盖入口文案、菜单/路由拦截和 API 鉴权（总控 API 与租户账号字段已完成）。
- [ ] 3. 实施最小权限边界并完成定向回归。
- [ ] 4. 运行完整前后端验证、构建与状态检查。

## 约束

- 前端隐藏、路由守卫与后端接口校验必须一致。
- 不修改无关的既有工作区改动。
