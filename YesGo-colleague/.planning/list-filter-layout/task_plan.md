# 列表筛选区统一计划

## 目标

完成所有已实现列表页面的筛选项顺序与筛选区样式统一。

## 阶段

- [x] 已完成：范围确认、设计评审、设计文档提交。
- [x] 已完成：实施计划编写。
- [x] 已完成：在隔离分支完成筛选区样式与筛选顺序调整，并提交 `b1f567b`。
- [x] 已完成：相关单测与生产构建验证；全量测试仅保留已确认的既有失败。

## 错误记录

| 错误 | 处理 |
| --- | --- |
| `session-catchup.py` 在 `$env:USERPROFILE\.codex\skills` 不存在 | 该工具安装于项目 `.codex/skills`，本次没有历史计划，已改为直接建立本任务计划。 |
| 前端全量基线测试失败 1 条 | `control-system-settings.spec.ts` 的提示文案断言与当前实现不一致；本任务未涉及该页面。 |
