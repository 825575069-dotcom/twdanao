# 进度

- 已创建隔离工作树 `codex/control-basic-settings-contact`。
- 已确认用户认可联系客服弹窗的信息卡片式设计，以及 Logo、Icon 的 OSS 上传方案。
- 已完成后端配置映射、OSS 上传目的、前端表单和客服弹窗实现。
- 验证通过：后端 `pytest -q` 377 项；迁移检查无变更；前端 `yarn test --runInBand` 315 项；`yarn build` 成功。
