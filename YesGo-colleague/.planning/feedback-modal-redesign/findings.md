# 调研记录

- 目标组件为 `web/src/components/support/AdminFloatingSupport.vue`。
- 现有实现已有四种反馈类型、500 字内容限制、选填联系方式、空内容校验、`aria-live` 错误区及 `feedback-*` 测试标识。
- 已确认视觉方向：方案 2 的左对齐紧凑表单样式；底部无分隔线；采用指定隐私文案；取消与提交按钮水平居中。
