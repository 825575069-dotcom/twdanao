# 进度日志

## 2026-07-20

- 用户确认第一期仅做双向心跳。
- 用户确认：心跳失败后自动重试 3 次；仍失败显示黑色半透明底部悬浮框，文案为“通信连接失败，请检查网络是否稳定”，并提供“重新连接”。
- 已完成架构调研并写入设计文档，待用户审阅后再进入实现。
- 设计文档已通过 `git diff --cached --check` 并提交：`23e933b docs: define websocket heartbeat recovery`。
