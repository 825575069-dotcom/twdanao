# 租户管理侧边栏空白页排查计划

## 目标

定位并修复总控后台侧边栏“租户管理相关”页面打开空白的问题，确保路由和页面可稳定渲染。

## 阶段

1. [x] 复现问题并核对菜单、路由与页面加载链。
2. [x] 确认根因，补充失败回归测试。
3. [x] 实施最小修复并完成定向验证。
4. [x] 完成完整前端验证并报告状态。
5. [x] 为后端菜单地址 `/control/tenant/list` 补齐 Vue Router 匹配并验证。
6. [x] 统一租户反馈菜单、权限种子与页面路由为 `/control/tenant/feedback`。
7. [x] 统一总控租户管理菜单、详情跳转与多标签路径为 `/control/tenant/*`。
8. [x] 审计所有总控租户管理入口与权限菜单路径一致性。
