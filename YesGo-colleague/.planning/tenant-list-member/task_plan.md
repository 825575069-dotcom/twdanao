# 租户列表与租户成员列表

## 目标

将总控“租户成员”替换为真实数据列表，并完善租户列表相关验证。

## 阶段

- [x] 1. 勘查现有路由、页面、模型与接口边界
- [x] 2. 明确范围并完成设计规格
- [x] 3. 编写实施计划并完成测试先行实现
- [x] 4. 执行后端、迁移和前端验证

## 关键决策

- 当前仅实现查询列表；不包含成员写操作或角色分配。
- 成员列表需面向总控跨租户查询，数据由 IAM 领域提供。

## 错误记录

| 问题 | 处理 |
| --- | --- |
| `session-catchup.py` 预期路径不存在 | 未发现可恢复脚本，改为读取现有活动计划和 Git 状态。 |
| 首次写入实施计划的补丁格式错误 | 修正所有补丁内容行前缀后成功写入计划文件。 |
| 隔离工作区未安装前端依赖 | 使用 Corepack Yarn 安装锁定依赖后，前端基线通过。 |
| 测试补丁误写入主工作区 | 已立即撤销仅由本任务引入的测试，并以绝对路径写入隔离分支。 |
| 两位测试成员使用相同手机号 | 命中租户内有效手机号唯一约束；改为不同手机号并使用前缀筛选。 |
