# 进度记录

## 2026-07-11

- 已记录浏览器评论及页面证据。
- 已定位布局与总控租户路由定义。
- 已补列表/详情面包屑的失败断言，并确认其分别因空根节点和缺少父级而失败。
- 已改为从 `menuKey` 的菜单标题派生父级：列表页只显示当前标题，详情页显示菜单父级加当前标题。
- 布局测试 21 项通过；浏览器刷新后已核对列表页只显示“租户列表”。
- 浏览器详情点击验证因超时重置未完成，后续以新增详情面包屑单元测试作为该路径的验证。
- 全量前端测试：180/182 通过；两个失败均来自尚未同步独立个人中心页面的旧弹窗测试。
- 构建：`breadcrumbRoot` 错误已解决，仍被 `profile-menu.spec.ts` 的 `ProfileAvatarEditor` 重复导入阻塞。
- 本任务已完成，未修改用户已有的其他工作区变更。
- 用户补充：详情页父级面包屑须可点击返回上级；已补失败测试，待实现。
- 已将父级面包屑转换为带菜单路径的按钮，当前页保持纯文本。
- 布局测试 21/21 通过：点击“租户列表”会跳转至 `/platform/tenants`。
- 构建仍仅因 `ProfileAvatarEditor` 重复导入失败；本任务未新增构建错误。
