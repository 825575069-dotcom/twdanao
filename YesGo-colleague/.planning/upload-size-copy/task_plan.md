# 上传大小配置文案任务计划

## 目标

将上传配置的单文件大小文案与说明统一为已确认的规范，并保持数字输入与保存契约不变

## 阶段

- [x] 需求与规格确认
- [x] 编写实施计划
- [x] 创建隔离 worktree
- [x] 测试先行修改与实现
- [x] 回归验证与提交

## 当前阶段

已合并至 dev，并完成 worktree 与功能分支清理

## 错误记录

- `session-catchup.py` 在预期路径不存在，未能自动恢复；已通过 `.planning/.active_plan` 确认存在无关的旧计划 `avatar-upload-debug`
- 两次大补丁因代码块行缺少补丁前缀而被拒绝，已改为简化计划格式后成功写入
- worktree 基线测试首次未启动：后端 Python 相对路径少一级，前端 worktree 缺少 `node_modules`；将复用主工作区依赖后重跑
- 依赖连接后后端基线通过（5 passed）；前端启动仍因 `@babel/parser` 缺失失败，需确认主工作区依赖完整性
- 按锁文件在 worktree 重装依赖后，前端仍因 `vitest/dist/cli.js` 缺失失败；这是第三次前端环境启动失败，按三次错误协议暂停并请求用户决定
- 整分支审查发现：读取接口对既有 `SystemConfig` 行优先返回持久化旧 `explain`，仅改定义会使存量环境继续展示旧文案；需增加持久化存量回归测试并修复读取逻辑
