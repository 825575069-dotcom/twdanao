# 进度记录

## 2026-07-16

- 已读取适用流程技能并完成第一轮代码库勘查。
- 已建立套餐功能持久化计划，当前等待产品规则确认后再形成设计。
- 已查阅 Dify 官方 Workflow API 与错误/限流说明，得到“平台配额与 Dify 平台限额分层处理”的结论。
- 已确认 Dify 工作流运行 API 直接返回 `total_tokens`，并可用 `workflow_run_id` 补查；真实 Token 结算方案具备 API 基础。

## 2026-07-17

- 恢复活动计划并复核当前实现；套餐入口仍是占位实现，未发现需兼容的既有套餐数据。
- 用户确认套餐按生效日起逐月重置，并设置生效日与到期日。
- 用户确认数据模型、结算规则、接口与后台页面设计；规格文档已写入，等待用户审阅后再编写实施计划。
- 已完成规格自检：未发现规格文本的格式或范围问题。`git diff --check` 暴露既有冲突标记（`sql/mysql/init/yesgo_init.sql`、`web/src/router/modules/control.ts`），故未执行提交。
- 已核验用户已清理冲突；开始编写实施计划。发现采购运行时当前退役，因此套餐将交付可被采购与其余智能体调用的配额/结算服务，而不在本期伪造 Dify 任务执行。
- 计划调研中一次 PowerShell 检索命令因反引号解析失败，已记录并改用安全的单引号模式。
- 实施计划已写入 `docs/superpowers/plans/2026-07-17-tenant-agent-package-implementation.md`，包含模型、领域服务、API、总控页面和验证步骤；待选择执行方式。
- 已完成实施计划自检：规格覆盖完整，未发现占位词、类型命名冲突或格式问题。规格文档现已包含在提交 `55f8ab8` 中；该提交也包含其他既有文件，本任务后续提交将严格按文件暂存。

## 2026-07-20

- 功能分支 `codex/tenant-agent-package` 已完成套餐、套餐配额、租户订阅快照与按工作流运行结算 Token 用量的模型、迁移、初始化 SQL、领域服务和总控 API。
- 已完成总控套餐管理页及租户详情的订阅/用量面板；保存校验、成功/失败反馈、无变更禁用保存均已补齐。
- 已将总控前端路径迁移为 `/control/...`，租户前端保持 `/tenant/...`，租户套餐固定为 `/control/tenant/package`；API 前缀未变。
- 路由迁移提交 `a49f0fe` 已通过 47 个 Vitest 文件、321 项测试、`vue-tsc -b`、Vite build 与 `git diff --check`。当前正在独立审查订阅面板与路由迁移，随后执行完整验收。
- 独立审查后已补齐：未来续订与当前订阅同时返回并展示、加载错误可见提示；旧 `/platform/...` 总控标签恢复时直接丢弃，绝不映射为新路径。相关提交为 `4a81f3e`、`f4deeae`。
- 前端全量验证：47 个测试文件、324 项测试通过；`vue-tsc -b` 与 Vite production build 通过。Django `makemigrations --check --dry-run` 输出 `No changes detected`。
- 后端全量 `pytest -q` 在约 132 项后出现既有控制台菜单权限用例失败（无角色账户期望 200，实际 403）；正在确认是否为本分支引入并完成其余失败诊断，不能将后端全量验收标记为通过。
- 已修复与总控页面加载直接相关的菜单权限缺口：已认证但未分配角色的总控用户可读取空菜单，提交 `1e6d937`；套餐模型、服务、控制台 API 与菜单权限相关测试合计 57 项通过。
- 后端全量继续后，`test_login_log_api.py` 的 4 个失败已确认是更早存在的测试夹具与登录日志平台审计权限的契约冲突：普通总控用户期望读取全平台审计日志，但接口正确要求 `IsPlatformAdmin`。本任务未降低审计权限，因此全量 `pytest -q` 仍不能标记为完全通过。
- 已将登录日志测试夹具修正为平台管理员（提交 `b21d716`），对应 7 项通过；后端全量命令在约 54% 后未返回汇总的运行器现象，通过后半段三批复验覆盖 61 + 37 + 35 项，均通过。结合全量前半段无失败，未发现本功能相关后端失败。
