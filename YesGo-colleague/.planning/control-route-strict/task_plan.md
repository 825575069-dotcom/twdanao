# 总控前端路由严格迁移

## 目标

将总控后台所有前端页面入口从 `/platform/...` 统一迁移为 `/control/...`，不保留旧路径兼容；租户资源固定在 `/control/tenant/...`。

## 阶段

- [x] 勘查当前路由、菜单、跳转与测试残留
- [ ] 编写失败回归测试并迁移路由骨架
- [ ] 迁移菜单、登录、状态页和页面直跳
- [ ] 清理旧标签恢复逻辑并完成全量扫描、测试和构建

## 约束

- 不新增或保留 `/platform/...` 作为总控前端路由。
- 不改变 `/api/control/...`、`/api/tenant/...` 或前端 `api/platform` 客户端模块名。
- 旧缓存中的 `/platform/...` 标签直接丢弃，不重定向或映射。
