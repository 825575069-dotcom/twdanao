# 状态页右侧图片区间距优化

## 目标

收紧管理后台状态页在桌面端左侧文案与右侧图片之间的视觉距离，同时保持现有移动端单列布局和状态功能不变。

## 阶段

1. [已完成] 定位共用状态页组件及现有布局参数。
2. [已完成] 完成并确认设计说明。
3. [待开始] 先写失败测试，再调整样式。
4. [待开始] 运行前端测试、构建与 Git 状态检查。

## 决策

- 仅修改 `web/src/views/common/AdminStatusPage.vue` 的桌面端布局参数。
- 缩小两栏间距，并限制右侧图片区块的最大宽度；不调整小于等于 900px 的单列断点行为。
- 设计说明已完成自检：无占位项，范围仅限共用状态页桌面端样式，验收项与实现范围一致。
